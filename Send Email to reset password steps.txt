1️⃣ Create / Use a Gmail App Password (IMPORTANT)

Google no longer allows normal Gmail passwords for SMTP.

Steps:

Go to 👉 Google Account

Security

Enable 2-Step Verification

✅ Step 1: Add an Authenticator App (REQUIRED)

Google only shows App passwords after an Authenticator app is added.

Click “Add authenticator app”

Install one of these on your phone:

Google Authenticator

Microsoft Authenticator

Scan the QR code

Confirm the 6-digit code

✔ Once done, the authenticator will appear in the list.

✅ Step 2: Reload the Security Page

After adding the authenticator:

Go back to Google Account → Security

Click 2-Step Verification again

Scroll down

👉 You should now see App passwords

If not visible directly, use this link:

https://myaccount.google.com/apppasswords

✅ Step 3: Create the App Password

Click App passwords

Select app → Mail

Select device → Other (Laravel)

Click Generate

Copy the 16-character password

⚠️ This password will be shown only once

✅ Step 4: Use It in Laravel .env
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=yourgmail@gmail.com
MAIL_PASSWORD=YOUR_APP_PASSWORD_HERE
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=yourgmail@gmail.com
MAIL_FROM_NAME="${APP_NAME}"


Then clear config:

php artisan config:clear
php artisan optimize:clear